/**
 * components/tables/transactions-table.tsx — Transactions data table.
 * Client-side filters (status, date range) + pagination over API response.
 * TODO (Phase 7 — task 7.3): implement.
 */

// TODO: implement (Phase 7 task 7.3)
export function TransactionsTable() {
  return (
    <div className="p-4 text-sm text-slate-500">
      TODO (Phase 7 task 7.3): transactions table with filters + pagination.
    </div>
  );
}
