/**
 * components/forms/payor-billing-form.tsx — Payor billing details form.
 * Structured address fields per DESIGN.md §3.3 (not a single free-text address).
 * Fields: first_name, last_name, email, phone?, address_line_one,
 *   address_line_two?, city_municipality, state_province_region,
 *   country_code (default "PH"), postal_code.
 * Plus brand selection state from the brand picker.
 * TODO (Phase 5 — task 5.3): implement.
 */

// TODO: implement (Phase 5 task 5.3)
export function PayorBillingForm() {
  return (
    <div className="p-4 text-sm text-slate-500">
      TODO (Phase 5 task 5.3): payor billing form.
    </div>
  );
}
