/**
 * (dashboard)/transactions/page.tsx — Transaction log tab.
 * Backed by dummy-data stub (lib/dlt-transaction-service.ts).
 * TODO (Phase 7 — task 7.3): implement.
 */

export default function TransactionsPage() {
  return (
    <main className="min-h-screen bg-slate-50 p-8">
      <h1 className="mb-4 text-2xl font-semibold text-slate-900">
        Transactions
      </h1>
      <p className="text-sm text-slate-500">
        TODO (Phase 7 task 7.3): implement transactions table with filters +
        pagination.
      </p>
    </main>
  );
}
